exports.HELP_MESSAGE = "Type /help to see this message again.";

exports.DISPLAY_HELP_UBUNTU ="Step 1: Install WireGuard\nStep 2: Run these commands to generate key pairs\n```umask 077\nwg genkey > wg0.key\nwg pubkey < wg0.key > wg0.pub```\nStep 3: Copy the public key\nStep 4: Run these commands to copy the keys to a secure location\n```sudo mkdir -p /etc/wireguard\nsudo mv wg0.key wg0.pub /etc/wireguard```\nStep 5: Create a new WireGuard configuration file and write the provided configuration into it using the following command\n```cat <<EOF | sudo tee /etc/wireguard/wg0.conf >/dev/null\n[Interface]\nPostUp = wg set %i private-key /etc/wireguard/wg0.key\nAddress = 192.168.6.11/32\n\n[Peer]\nPublicKey = u2t3q+QQp6L1B77nqLewXaSVGycTPn7yIf7909qEAUI=\nAllowedIPs = 192.168.0.0/16\nEndpoint = 49.205.217.220:13231\nEOF\n```\nReplace `wg0.key` with the appropriate path if it's different."

exports.DISPLAY_HELP = "\nType ```/help-windows``` if you are using Machine with windows OS\nType ```/help-mac``` if you are using machine with Mac OS\nType ```/help-ubuntu``` if you are using machine wiht Ubuntu OS"